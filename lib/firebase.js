// Firebase stub: prevents build-time import errors while we run without Firebase.
// If any runtime code still tries to use these, it should be refactored to lib/db.js.
export const app = null;
export const auth = null;
export const db = null;
export const storage = null;

export default { app, auth, db, storage };
